export default function LearnLoading() {
  return (
    <section className="curriculum-state" aria-busy="true" aria-live="polite">
      <p className="eyebrow">Loading</p>
      <h1>Loading curriculum browser</h1>
      <p>Preparing schools, programmes, modules, and pathways.</p>
    </section>
  );
}
